openai>=1.0.0
chromadb>=0.4.0
python-dotenv>=1.0.0
pydub>=0.25.0
setuptools>=68.0.0
wheel>=0.40.0
firebase-admin>=6.0.0
requests>=2.28.0
python-multipart>=0.0.6
langchain>=0.0.1
langchain-openai>=0.0.1
langgraph>=0.0.1
faiss-cpu>=1.7.0
uvicorn[standard]>=0.20.0
fastapi>=0.100.0


# py -3.12 -m venv venv312
# venv312\Scripts\Activate
# # Upgrade pip trước
# pip install --upgrade pip

# pip install setuptools wheel

# pip install --force-reinstall --no-cache-dir -r requirements.txt

########## MANUAL STEP #############

# Basic packages first (least likely to fail)
# pip install --no-cache-dir "python-dotenv>=1.0.0"
# pip install --no-cache-dir "requests>=2.28.0" 
# pip install --no-cache-dir "fastapi>=0.100.0"
# pip install --no-cache-dir "uvicorn[standard]>=0.20.0"

# pip install --no-cache-dir "langchain>=0.0.1"
# pip install --no-cache-dir "langchain-openai>=0.0.1"
# pip install --no-cache-dir "langgraph>=0.0.1"
# pip install faiss-cpu>=1.7.0


# Firebase and OpenAI
# pip install --no-cache-dir "firebase-admin>=6.0.0"
# pip install --no-cache-dir "openai>=1.0.0"

# Audio processing
# pip install --no-cache-dir "pydub>=0.25.0"

# Most complex packages last
# pip install --no-cache-dir --only-binary=all "numpy>=1.24.0,<2.0"  # Use prebuilt wheel
# pip install --no-cache-dir "chromadb>=0.4.0"