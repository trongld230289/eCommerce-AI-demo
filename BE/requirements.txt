openai>=1.0.0
chromadb>=0.4.0
python-dotenv>=1.0.0
numpy==1.24.3
pydub>=0.25.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
setuptools>=68.0.0
wheel>=0.40.0
firebase-admin>=6.0.0
requests>=2.28.0


# # Upgrade pip trước
# pip install --upgrade pip

# # Cài các package cơ bản trước (ít lỗi nhất)
# pip install --no-cache-dir requests
# pip install --no-cache-dir "numpy==1.24.3"
# pip install --no-cache-dir "openai>=1.0.0"
# pip install --no-cache-dir "firebase-admin>=6.0.0"

# # Cài packages phức tạp hơn
# pip install --no-cache-dir "pydub>=0.25.0"

# # Cuối cùng cài chromadb (thường lỗi nhất)
# pip install --no-cache-dir "chromadb>=0.4.0"